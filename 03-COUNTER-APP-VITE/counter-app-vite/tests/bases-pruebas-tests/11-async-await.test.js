import { getImagen } from "../../src/base-pruebas/11-async-await"

describe('Pruebas en 11', () =>{
    test('getImagen debe de retorna un URL de la imagen', ()=> {
        // const url = await getImagen()
        // console.log(url)
        // No anda la prueba del await por que hay que actualizar el node
    })
})